name: Build and Push Zocial Image

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to build"
        required: true
        default: "main"
        type: choice
        options:
          - main
          - dev
          - nodejs

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: sh

    steps:
      - name: Checkout selected branch
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}

      - name: Generate timestamp
        run: echo "TAG=$(date +%Y%m%d-%H%M)" >> $GITHUB_ENV

      - name: Set image tag
        run: |
          if [ "${{ inputs.branch }}" = "dev" ]; then
            echo "IMAGE_TAG=dev" >> $GITHUB_ENV
          elif [ "${{ inputs.branch }}" = "nodejs" ]; then
            echo "IMAGE_TAG=nodejs" >> $GITHUB_ENV
          else
            echo "IMAGE_TAG=latest" >> $GITHUB_ENV
          fi

      - name: Login to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login \
            -u "${{ secrets.DOCKER_USERNAME }}" \
            --password-stdin

      - name: Build image
        run: |
          docker build \
            -t domoel/enafore:${IMAGE_TAG} \
            -t domoel/enafore:${IMAGE_TAG}-${TAG} \
            .

      - name: Push image
        run: |
          docker push domoel/enafore:${IMAGE_TAG}
          docker push domoel/enafore:${IMAGE_TAG}-${TAG}
